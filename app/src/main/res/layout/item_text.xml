<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ll_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:ignore="MissingPrefix">

    <TextView
        android:id="@+id/tv_option"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:paddingVertical="7dp"
        android:textSize="14sp"
        app:bl_corners_radius="100dp"
        app:bl_selected_stroke_color="@color/colorPrimary"
        app:bl_selected_textColor="@color/colorPrimary"
        app:bl_stroke_width="1dp"
        app:bl_unSelected_stroke_color="@color/red_f54860"
        app:bl_unSelected_textColor="@color/black"
        tools:text="圆肩驼背" />

</FrameLayout>